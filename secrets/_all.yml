# start of Meshtastic configure yaml
config:
  mqtt:
    encryptionEnabled: false
  security:
    adminKey:
    - 'base64:aaaaaaaaaaa='

module_config:
  mqtt:
    address: 0.0.0.0
    password: xxxxxxxxxxxs
    root: msh/EU_868
    username: username
